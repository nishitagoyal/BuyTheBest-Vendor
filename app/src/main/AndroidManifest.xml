<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.something.vendorapp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/btb_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/btb_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VendorApp">

        <activity android:name=".activity.AddCategoryActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".activity.AddItemActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/Theme.VendorApp.NoActionBar"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".activity.SplashActivity"
            android:theme="@style/Theme.VendorApp.NoActionBar"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.VendorApp.NoActionBar"
            android:screenOrientation="portrait"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>